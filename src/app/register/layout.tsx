export const metadata = {
    title: "Register - Go Planeje",
    description: "Register a account in Go Planeje.",
  };
  
  export default function LoginLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return <>{children}</>;
  }
  